<p><em>*add additional stock of an item you have at other locations to this location. This does not remove or change stock of the item at the other location(s).</em></p><br>

<%= form_for item, as: :existing_item, url: location_items_path(location) do |f| %>
   <div class="input-field container">
      <%= f.collection_select :id, user_items, :id, :name %>
      <%= f.label :id, "Existing Items" %>
   </div>
      <%= fields_for location_item do |loc_item| %>
         <%= loc_item.hidden_field :location_id %>
         <div class="row">     
            <%= loc_item.label :quantity, "Quantity: " %>
            <div class="input-field inline">
               <%= loc_item.number_field :quantity, step: 1 %>
            </div>
         </div>
      <% end %>
   <%= f.submit "Add Existing Item to #{location.name}", class: "btn brown darken-1" %>
<% end %>
<h3 class="center">--OR--</h3> 